// Vue Essaim — stub en attente d'implémentation (remplacé par l'agent de build).

import type { ViewProps } from './shared';

export default function Essaim(_props: ViewProps) {
  return <div className="mc-view-loading">Vue Essaim en construction…</div>;
}

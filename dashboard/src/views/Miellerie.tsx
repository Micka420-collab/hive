// Vue Miellerie — stub en attente d'implémentation (remplacé par l'agent de build).

import type { ViewProps } from './shared';

export default function Miellerie(_props: ViewProps) {
  return <div className="mc-view-loading">Vue Miellerie en construction…</div>;
}

// Vue Sante — stub en attente d'implémentation (remplacé par l'agent de build).

import type { ViewProps } from './shared';

export default function Sante(_props: ViewProps) {
  return <div className="mc-view-loading">Vue Sante en construction…</div>;
}

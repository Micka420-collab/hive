// Vue Chronique — stub en attente d'implémentation (remplacé par l'agent de build).

import type { ViewProps } from './shared';

export default function Chronique(_props: ViewProps) {
  return <div className="mc-view-loading">Vue Chronique en construction…</div>;
}
